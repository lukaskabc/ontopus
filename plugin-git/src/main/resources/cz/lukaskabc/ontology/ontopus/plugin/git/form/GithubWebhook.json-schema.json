{
  "type": "object",
  "properties": {
    "webhook": {
      "$translationRoot": "ontopus.plugin.git.webhook.github",
      "type": "object",
      "properties": {
        "uri": {
          "type": ["null", "string"]
        },
        "secret": {
          "type": "string"
        },
        "event": {
          "type": "string",
          "enum": ["push", "create"]
        },
        "ref": {
          "type": "string"
        }
      },
      "required": ["secret", "event", "ref"]
    }
  },
  "required": ["webhook"]
}
