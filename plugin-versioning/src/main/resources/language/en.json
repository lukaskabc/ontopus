{
  "ontopus.core.service.OntologyVersioningService": {
    "OntologyVersionResolvingService": {
      "name": "Extract from ontology annotations",
      "description": "Versions the ontology based on ontology annotations.",
      "form": {
        "title": "Resolve ontology version and version IRI from ontology annotations",
        "description": "OntoPuS will resolve the values for specified properties on each ontology release, it is the responsibility of the ontology author to change the values before releasing a new version. \nSelect the annotation properties that will be used to determine the version and version IRI of the ontology.",
        "fields": {
          "version": {
            "title": "Version",
            "description": "The version of the ontology e.g. 1.2.3 ([semantic versioning](https://semver.org/)), 2024-06-01 (date based versioning) or other preferred version scheme."
          },
          "versionIri": {
            "title": "Version IRI",
            "description": "The version IRI of the ontology. Unique IRI for this specific ontology version."
          }
        }
      }
    },
    "GeneralVersioningService": {
      "name": "User defined version",
      "description": "Accepts user defined text as version."
    },
    "DateVersioningService": {
      "name": "Date based version",
      "description": "Uses current date as the version (YYYY-MM-DD)."
    }
  }
}
